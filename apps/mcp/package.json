{
  "name": "@in-org-quicko/skillset-mcp",
  "version": "1.0.0",
  "description": "MCP server for searching, installing, and publishing Skills on a Skillset Registry.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/org-quicko/skillset.git",
    "directory": "apps/mcp"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "skillset-mcp": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run src/build.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "discovery": "bun run src/write-discovery.ts",
    "package:mcpb": "bun run build && bunx @anthropic-ai/mcpb pack . skillset-mcp.mcpb"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@in-org-quicko/skillset-installer": "workspace:*",
    "@in-org-quicko/skillset-shared": "workspace:*",
    "@types/bun": "^1.4.0",
    "fflate": "^0.8.3",
    "typescript": "^5.7.2",
    "zod": "^4.4.3"
  }
}
