# Copy to .env and fill in before running `docker compose up`.

# Postgres connection string. Compose provisions Postgres 18 for you at this URL.
DATABASE_URL=postgres://postgres:postgres@postgres:5432/skill_registry

# Signing secret for session JWTs. Required — the app refuses to start without it.
# Generate one with: openssl rand -base64 32
JWT_SECRET=

# Port the application listens on.
PORT=3000

# Object storage (S3 or S3-compatible). STORAGE_BUCKET is required — publishing
# presigns uploads against it, so the app refuses to start without one.
STORAGE_BUCKET=
STORAGE_REGION=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=
# Optional — set for a self-hosted S3-compatible endpoint (e.g. MinIO).
STORAGE_ENDPOINT=
# Optional — the endpoint embedded in presigned URLs, if the browser reaches
# storage at a different address than the API does (e.g. the API talks to
# MinIO over a Docker network hostname, but the browser needs localhost).
# Defaults to STORAGE_ENDPOINT.
STORAGE_PUBLIC_ENDPOINT=
