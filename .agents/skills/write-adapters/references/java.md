# Java adapters

## Converter

Import `Converter` from `company.quicko.core.converter` and
`ConverterException` from `company.quicko.core.exceptions`.

## Exception

Use `company.quicko.core.exceptions.AdapterException`. Declare
`throws AdapterException` on adapter interface methods.
