{
  "name": "skill-registry",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "dev": "bun --filter @skill-registry/api dev",
    "build": "bun --filter @skill-registry/web build",
    "test": "bun run --filter '@skill-registry/shared' --filter '@skill-registry/api' test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
